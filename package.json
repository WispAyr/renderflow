{
  "name": "renderflow",
  "version": "0.1.0",
  "description": "Content-to-Screen Platform with AI Enhancement",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "server": "pnpm --filter @renderflow/api dev",
    "studio": "pnpm --filter @renderflow/web dev",
    "render": "pnpm --filter @renderflow/renderer dev"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "prettier": "^3.2.0",
    "eslint": "^9.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/WispAyr/renderflow.git"
  },
  "author": "Local Connect Systems",
  "license": "MIT"
}
